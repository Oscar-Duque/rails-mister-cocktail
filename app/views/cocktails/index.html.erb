<p id="notice"><%= notice %></p>

<h1>Cocktails</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cocktails.each do |cocktail| %>
      <tr>
        <td><%= cocktail.name %></td>
        <td><%= link_to 'Show', cocktail %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= cl_image_tag("v1605294244",width: 400, height: 300, crop: :fill) %>
<%= cl_image_tag("v1605294264",width: 150, height: 150, crop: :thumb, gravity: :face) %>

<br>

<%= link_to 'New Cocktail', new_cocktail_path %>
